#include <iostream>
#include <string>

using namespace std;
	void plusa(string k){
		k=k+'a';
		cout << k;
	}
int main() {
  string k = "kuty";
  // write a function that gets a string as an input
  // and appends an 'a' character to its end
  	  	 plusa(k);

  return 0;
}
