#include <iostream>
#include <string>

using namespace std;
  void plusa(string y){
    y+='a';
	}
int main() {
  string k = "kutyyy";
  // write a function that gets a string as an input
  // and appends an 'a' character to its end
  plusa(k);
  return 0;
}
